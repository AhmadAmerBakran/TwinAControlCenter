name: Windows installer

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/**'
      - 'improve/**'
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'installer/**'
      - 'scripts/**'
      - 'docs/**'
      - '.github/workflows/installer.yml'
      - 'TwinAControlCenter.sln'
      - 'README.md'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'installer/**'
      - 'scripts/**'
      - 'docs/**'
      - '.github/workflows/installer.yml'
      - 'TwinAControlCenter.sln'
      - 'README.md'

permissions:
  contents: write

concurrency:
  group: windows-installer-${{ github.ref }}
  cancel-in-progress: true

jobs:
  installer:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Setup Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Privacy check
        shell: powershell
        run: .\scripts\privacy-check.ps1

      - name: Install Inno Setup
        shell: powershell
        run: choco install innosetup --yes --no-progress

      - name: Build installer
        shell: powershell
        run: |
          $packageVersion = (Get-Content '.\frontend\package.json' -Raw | ConvertFrom-Json).version
          $version = if ($env:GITHUB_REF -eq 'refs/heads/main') { $packageVersion } else { "$packageVersion-dev" }
          .\scripts\package-installer.ps1 -Version $version

      - name: Installer smoke test
        shell: powershell
        run: .\scripts\installer-smoke-test.ps1

      - name: Upload installer artifact
        uses: actions/upload-artifact@v6
        with:
          name: TWIN-A-Control-Center-Installer
          path: artifacts/installer/*.exe
          if-no-files-found: error
          retention-days: 14

      - name: Publish GitHub Release asset
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: powershell
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $asset = (Get-ChildItem 'artifacts\installer\*.exe' | Select-Object -First 1).FullName
          if (-not $asset) { throw 'Installer asset was not produced.' }

          $packageVersion = (Get-Content '.\frontend\package.json' -Raw | ConvertFrom-Json).version
          $tag = "v$packageVersion"

          gh release view $tag --json tagName *> $null
          $releaseExists = $LASTEXITCODE -eq 0

          if ($releaseExists) {
            gh release upload $tag $asset --clobber
          }
          else {
            gh release create $tag $asset --target $env:GITHUB_SHA --generate-notes --title "TWIN A Control Center $tag"
          }
